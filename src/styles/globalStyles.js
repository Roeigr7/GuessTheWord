import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  upContainer: {
    flex: 1,
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
  upContainer: {
    flex: 1,
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
  downContainer: {
    width: '100%',
    flex: 2,
    alignItems: 'center',
    justifyContent: 'center',
  },

  bgImage: {
    flex: 1,
    resizeMode: 'cover',
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
